#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define INF 0x3f3f3f3f
#define pb push_back

char c;
ll q, s, t, i;

void solve()
{
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> q;
    while (q--)
    {
        cin >> c;
        if (c == 'A')
        {
            cin >> s >> t;
        }
        else
        {
            cin >> i;
        }
        solve();
    }
}